// const app = require('../src/app');
// const request = require('supertest')(app);

// const loginUser = require('../src/lib/testFunctions');

// // describe('GET /api/v1/difficulty', function () {
// //   // var agent;
// //   // beforeEach(function (done) {
// //   //   loginUser.login(request, function (loginAgent) {
// //   //     agent = loginAgent;
// //   //     done();
// //   //   });
// //   // });
// //   // it('should allow access to difficulty when logged in', function (done) {
// //   //   var req = request.get('/api/v1/difficulty');
// //   //   req.expect(200, done);
// //   // });
// //   // it('should require authorization', async () => {
// //   //   request.get('/api/v1/difficulty').expect('Content-Type', /json/).expect(403);
// //   // });
// //   // token = loginUser();
// //   // it('should respond with an array of difficulty levels', async () => {
// //   //   const response = await request.get('/api/v1/difficulty').set('authorization', token).expect(200).expect('Content-Type', /json/);
// //   //   expect(response.body.length.toBeGreaterThan(0));
// //   // });
// // });
